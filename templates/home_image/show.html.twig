{% extends 'base.html.twig' %}

{% block title %}HomeImage{% endblock %}

{% block body %}
    <h1>HomeImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_image.id }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ home_image.fileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_image_index') }}">back to list</a>

    <a href="{{ path('home_image_edit', {'id': home_image.id}) }}">edit</a>

    {{ include('home_image/_delete_form.html.twig') }}
{% endblock %}
