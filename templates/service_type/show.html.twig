{% extends 'base.html.twig' %}

{% block title %}ServiceType{% endblock %}

{% block body %}
    <h1>ServiceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ service_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_type_index') }}">back to list</a>

    <a href="{{ path('service_type_edit', {'id': service_type.id}) }}">edit</a>

    {{ include('service_type/_delete_form.html.twig') }}
{% endblock %}
