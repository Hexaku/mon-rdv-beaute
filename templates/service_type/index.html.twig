{% extends 'base.html.twig' %}

{% block title %}ServiceType index{% endblock %}

{% block body %}
    <h1>ServiceType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service_type in service_types %}
            <tr>
                <td>{{ service_type.id }}</td>
                <td>{{ service_type.type }}</td>
                <td>
                    <a href="{{ path('service_type_show', {'id': service_type.id}) }}">show</a>
                    <a href="{{ path('service_type_edit', {'id': service_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('service_type_new') }}">Create new</a>
{% endblock %}
