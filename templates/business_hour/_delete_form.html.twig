<form method="post" action="{{ path('business_hour_delete', {'id': business_hour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business_hour.id) }}">
    <button class="btn">Delete</button>
</form>
