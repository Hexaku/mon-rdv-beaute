{% extends 'base.html.twig' %}

{% block title %}Oubli du mot de passe{% endblock %}

{% block body %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    <div class="container-flex">
        <h1 class="title">Oubli du mot de passe</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Continuer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
