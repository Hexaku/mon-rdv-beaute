{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <div class="contact-form">
        <h2 class="title">Tous les articles</h2>
        <a href="{{ path('article_new') }}"><button class="btn btn-link">Écrire un nouvel article</button></a>
        {% for article in articles %}
        <div class="card">
            <div class="card-body">
                <h3>{{ article.title }}</h3>
                <p>{{ article.content }}</p>
                <a href="{{ path('article_show', {'id': article.id}) }}"><button class="btn btn-primary">Lire l'article</button></a>
            </div>
        </div>
        {% else %}
            <p>Il n'y a pas d'articles !</p>
        {% endfor %}
    </div>
{% endblock %}
